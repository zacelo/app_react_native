import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container:{
    backgroundColor:'grey',
    shadowColor: 'black',
    textShadowOffset: {height:15, width:3},
    elevation:15,
    shadowOpacity:1,
    shadowRadius:1
  }

})